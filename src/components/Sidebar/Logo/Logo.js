import React, { Component } from 'react';
import styles from './Logo.module.css';

class Logo extends Component {
  render() {
    return (
      <div className={styles.container}>P</div>
    );
  }
}

export default Logo;
